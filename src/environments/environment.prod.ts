const baseUrl = 'http://34.36.129.27';

const UrlUsuarios=`${baseUrl}/usuarios`;
const UrlPerfiles = `${baseUrl}/perfiles`; //Aqui va la url de ingress
const UrlIndicador = `${baseUrl}/indicadores`;
export const environment = {
  production: true,
  UrlUsuarios,
  UrlPerfiles,
  UrlIndicador,
};


